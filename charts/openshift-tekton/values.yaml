---
helper-operator:
  operators:
    redhat-tekton-operator:
      enabled: true
      syncwave: '0'
      namespace:
        name: default
        create: true
      subscription:
        channel: latest
        approval: Automatic
        operatorName: openshift-pipelines-operator-rh
        source: redhat-operators
        sourceNamespace: openshift-marketplace
      operatorgroup:
        create: false

helper-status-checker:
  enabled: false

  checks:

    - operatorName: openshift-pipelines-operator-rh
      namespace:
        name: default
      syncwave: 3

      serviceAccount:
        name: "status-checker-tekton"
