apiVersion: machine.openshift.io/v1beta1
kind: MachineSet
metadata:
  annotations:
    machine.openshift.io/memoryMb: {{ $value.vsphere.memoryMiB }}
    machine.openshift.io/vCPU: {{ $value.vsphere.numCPUs }}
  name: {{ $value.machine-set.prefix }}-{{ $value.machine-set.role }}-0
  namespace: openshift-machine-api
  labels:
    machine.openshift.io/cluster-api-cluster: {{ $value.vsphere.prefix }}
spec:
  replicas: {{ $value.machine-set.replicas }}
  selector:
    matchLabels:
      machine.openshift.io/cluster-api-cluster: {{ $value.vsphere.prefix }}
      machine.openshift.io/cluster-api-machineset: {{ $value.machine-set.prefix }}-{{ $value.machine-set.role }}-0
  template:
    metadata:
      labels:
        machine.openshift.io/cluster-api-cluster: {{ $value.machine-set.prefix }}
        machine.openshift.io/cluster-api-machine-role: {{ $value.machine-set.role }}
        machine.openshift.io/cluster-api-machine-type: {{ $value.machine-set.role }}
        machine.openshift.io/cluster-api-machineset: {{ $value.machine-set.prefix }}-{{ $value.machine-set.role }}-0
    spec:
      lifecycleHooks: {}
      metadata: {}
      providerSpec:
        value:
          numCoresPerSocket: {{ $value.vsphere.numCoresPerSocket }}
          diskGiB: {{ $value.vsphere.diskGiB }}
          snapshot: ''
          userDataSecret:
            name: {{ $value.vsphere.userDataSecret }}
          memoryMiB: {{ $value.vsphere.memoryMiB }}
          credentialsSecret:
            name: {{ $value.vsphere.credentialsSecret }}
          network:
            devices:
              - networkName: {{ $value.vsphere.networkName }}
          metadata:
            creationTimestamp: null
          numCPUs: {{ $value.vsphere.numCPUs }}
          kind: VSphereMachineProviderSpec
          workspace:
            datacenter: {{ $value.vsphere.datacenter }}
            datastore: {{ $value.vsphere.datastore }}
            folder: {{ $value.vsphere.folder }}
            resourcePool: {{ $value.vsphere.resourcePool }}
            server: {{ $value.vsphere.server }}
          template: {{ $value.machine-set.prefix }}-rhcos-generated-region-generated-zone
          apiVersion: machine.openshift.io/v1beta1
